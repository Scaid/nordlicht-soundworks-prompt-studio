# Version 4.4.2 – Workspace Accordion Fix

- Fixed Knowledge Workspace collapsing after opening Genre Intelligence or other modules not listed in the legacy workspace map
- Workspace ownership is now resolved from the actual sidebar DOM structure
- Added a safe fallback to the legacy workspace map
- Prevents newly added modules from opening the wrong workspace group
- Keeps the active workspace expanded while navigating between its modules
